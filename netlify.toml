[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  NITRO_PRESET = "netlify"

# Sem redirect SPA manual. O preset Nitro gera a função SSR e o fallback
# correto para ela durante o build, mantendo rotas e /api ativos.
